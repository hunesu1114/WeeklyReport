# docker compose 에서 쓰는 값. 그대로 .env 로 복사해서 쓰면 된다.
POSTGRES_DB=weekly_report
POSTGRES_USER=weekly
POSTGRES_PASSWORD=weekly

# 호스트에 노출할 포트
FRONTEND_PORT=3000
BACKEND_PORT=8081
DB_PORT=5434

# 백엔드를 직접 호출하는 개발 서버 오리진
APP_CORS_ALLOWED_ORIGINS=http://localhost:5174,http://localhost:3000
