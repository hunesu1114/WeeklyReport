server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: weekly-report
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/weekly_report}
    username: ${SPRING_DATASOURCE_USERNAME:weekly}
    password: ${SPRING_DATASOURCE_PASSWORD:weekly}
    hikari:
      maximum-pool-size: 10
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
  flyway:
    enabled: true
    baseline-on-migrate: true
  jackson:
    default-property-inclusion: non_null
  servlet:
    multipart:
      max-file-size: 5MB

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5174,http://localhost:3000}
  jwt:
    # 비워두면 기동할 때 임시 키를 만든다. 재시작마다 로그인이 풀리므로
    # 운영에서는 반드시 채운다.  openssl rand -base64 32
    secret: ${APP_JWT_SECRET:}
    ttl: ${APP_JWT_TTL:12h}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    com.khs.weeklyreport: INFO
